#!/bin/bash

if [[ $# -eq 0 ]]; then
    echo "Usage: $0 <YouTubeID> [destination]"
    exit 1
fi

if [ -n "$2" ]; then
  yt-dlp --sub-lan=en --write-auto-sub --skip-download -o "$2" $1
else
  yt-dlp --sub-lan=en --write-auto-sub --skip-download $1
fi
